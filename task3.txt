
Status: task 3 complete.
You are refactoring a Vite+React+TS+Tailwind project.

GOAL
Purge legacy `.btn-cta` and unify all button-like anchors with the shared LinkButton (primary|ghost). Do not change texts, hrefs, routes, or behavior.

ACTIONS
1) Replace any <a> styled as a button (rounded/inline-flex/px-*/py-*/role="button") with <LinkButton variant="primary" or "ghost">, preserving href/target/aria/data-*; add rel="noopener noreferrer" if target="_blank".
2) Replace any <button className="btn-cta"> with <ButtonPrimary> preserving handlers.
3) Remove dead CSS for .btn-cta (base/hover/active/--sm) from global styles.
4) Ensure homepage CTAs also use LinkButton consistently.

CONSTRAINTS
- Do not alter copy/i18n.
- No new colors/libs.
- Keep non-CTA text links as plain links.

ACCEPTANCE
- grep "btn-cta" returns 0 in src/.
- grep -E '<a[^>]+(btn-cta|role="button"|inline-flex|rounded|px-[2345678]|py-[2345678])' returns only non-CTA text links.
- Build/typecheck succeed.

COMMIT
chore(ui): purge legacy btn-cta and unify anchor CTAs with LinkButton

...................................................................................................


task4
Summary of changes
- Added shared anchor CTA: src/components/ui/LinkButton.tsx (variants: primary | ghost). Shape and focus ring identical to ButtonPrimary (focus-visible:ring-2 focus-visible:ring-brand-primary/50).
- Migrated all homepage CTAs to LinkButton (Hero + “Выбор дня” + header CTA).
- Unified affiliate tracking: src/components/misc/AffiliateLink.tsx now renders LinkButton internally and preserves appendStoredParams, tracking event, target="_blank" + rel="nofollow sponsored noopener", and size classnames.
- Purged legacy .btn-cta classes and CSS; removed unused Hero components (Hero.tsx, HomeHero.tsx).

Verification
- grep "btn-cta" in src → 0 matches.
- All Home CTAs use ButtonPrimary/ButtonGhost/LinkButton only.
- Focus ring consistent across LinkButton/Button components.
- pnpm typecheck: OK. pnpm build: OK.

Commit
chore(ui): purge legacy btn-cta; add LinkButton and use on Home; remove old Hero; ensure consistent focus ring

Next task request
Please sweep the entire repo for any remaining button-like <a> outside the Home route and convert them to the shared <LinkButton>:

GOAL
Unify all anchor CTAs across the app with <LinkButton> (primary | ghost). Do not change copy, hrefs, routes, or behavior.

ACTIONS
1) Replace any anchor visually styled as a button (rounded/inline-flex/px-*/py-*/role="button" or legacy styles) with <LinkButton variant="primary" or "ghost">, preserving href/target/aria/data-*; add rel="noopener noreferrer" when target="_blank".
2) Keep normal text links as plain anchors (do not convert them).
3) Ensure keyboard focus ring matches ButtonPrimary via LinkButton styling.

CONSTRAINTS
- No new libraries/colors/tokens.
- Do not alter texts or navigation.

ACCEPTANCE
- Repo-wide: no ".btn-cta" usage; no anchor masquerading as a button remains.
- Grep checks pass:
  - git grep -n 'btn-cta' → no results
  - git grep -nE '<a[^>]+(role="button"|inline-flex|rounded|px-[2345678]|py-[2345678])' returns only true text links (if any remain, justify).
- Build and typecheck succeed.

Commit for this sweep
chore(ui): unify anchor CTAs with LinkButton across the app