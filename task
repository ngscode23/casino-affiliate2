ТЫ лузер проавлил этот таска 

TASK: Lockdown fix for catalog/home grid & container (diagnose → enforce → verify)
Phase 0 — Preflight (mandatory)
Выведи краткий план (5–8 строк): какие компоненты трогаешь и почему. Дальше действуй без вопросов.
Phase 1 — Diagnose (mandatory, no code yet)
Короткий отчёт, почему:

На каталоге карточки занимают всю ширину экрана и контейнер не ограничивает до ~1280.
На главной карточки тоже раздуты.

Укажи:

какие родительские обёртки дают неправильную ширину (слишком широкие/узкие, вложенные контейнеры, дубли контейнеров, не тот флаг «оборачивать контейнером»);
какие правила ширины или фикс-ширины у ближайших родителей/карточек мешают сетке (любые фиксированные ширины/макс-ширины/fit-width/basis);
есть ли дубли версий карточки (встроенная локальная vs каноническая) и конфликтующие стили;
применяются ли responsive-классы (если нет — почему).

Максимум 10 строк, конкретные причины.
Phase 2 — Enforce the layout
Цели, неизменяемые:

Обе страницы: один центрированный контейнер порядка 1280 px на десктопе.
Каталог: 4/3/2 колонки по брейкпоинтам; карточки одного размера; нормальные отступы.
Главная: тот же контейнер; карточки не гиганты.

Правила:

Правь существующие компоненты.
Разрешено создание/удаление только если это строго необходимо для устранения дубликатов/конфликтов. В этом случае:

делай safe-delete (перемещение в корзину проекта),
добавь Justification Note: почему без этого нельзя, что именно удалено/создано, как откатить.


Никаких новых CSS-файлов и inline-стилей.
Данные/провайдеры не трогать.
Используй единственную каноническую карточку. Убери локальные дубликаты или переключи на канон.

Что именно обеспечить в результате:

Контейнер вокруг секции есть в одном экземпляре и реально ограничивает ширину; нет вложенных конкурирующих контейнеров.
Родители грида не имеют фикс-ширин и не ломают ширину (никаких кастомных width/max-width у ближайших родителей).
Грид реально даёт 2/3/4 колонки, а карточки не задают себе фикс-ширину и не «выламывают» сетку.
Медиа в карточке с постоянным соотношением сторон; CTA прижат вниз.

Phase 3 — Verify (mandatory)
Выведи:

измеренную ширину контейнера на десктопе (~1280);
число колонок на десктопе/планшете/мобиле;
список убранных фикс-ширин/дубликатов;
подтверждение, что нет лишних контейнеров.

Acceptance criteria

На обеих страницах контейнер центрирован и ~1280 на десктопе.
Каталог: 4/3/2 колонки, карточки одинаковой геометрии, без растяжения на всю ширину экрана.
Нет фикс-ширин на карточке и её ближайших родителях.
Нет дублей карточек/гридов; используется каноническая карточка.
Нет новых файлов стилей, нет inline.
Консоль без ошибок гидрации/импорта.

Hard constraints

Do exactly this. Любая «улучшайзинг-самодеятельность» запрещена.
Если нужен create/delete — только с Justification Note и safe-delete.


Если опять начнёт играть в художника, добавь в конец:
“If container width is still >1280 or cards still span full screen, revert and try again. No partial fixes accepted.”
И да, на всякий случай добей его проверками локально:

убери все фикс-ширины на ближайших родителях карточек,
не допускай двух контейнеров вокруг одной и той же секции,
выкинь локальные версии ProductCard внутри грида.

Хватит кормить его токенами за Excel-эстетику.
