*** Begin Patch
*** Add File: apps/web-next/app/products/images.ts
+import { normalizeImageUrl } from "./[slug]/data";
+
+type Row = Record<string, unknown> | null | undefined;
+
+export function imageUrlFrom(row: Row): string | null {
+  if (!row) return null;
+  const raw =
+    (typeof (row as any)?.thumbnail === "string" && (row as any).thumbnail) ||
+    (typeof (row as any)?.thumbnail_path === "string" && (row as any).thumbnail_path) ||
+    null;
+  if (!raw) return null;
+  const trimmed = raw.trim();
+  if (!trimmed) return null;
+  return normalizeImageUrl(trimmed);
+}
+
*** End Patch
